void main() {
  ///Raw strings are useful when you want to define a String that has a lot of special characters.
  ///We can create a raw string by prefixing it with r .
  var gfg = r'This is a raw string';
  print(gfg);
}
